// Exercise 1: Calculate the Sum of Two Numbers

a = intiger
b = intiger

c = sum

a + b = c

if
	 a or b is not an intiger display "Please enter a valid number"

else
	Display "C is the sum"

// Exercise 2: Check if a Number is Even or Odd

a = intiger

if a is not an intiger display "please enter valid number"

else
	a / 2
if true
	display "A is even"
else
	display "A is odd"

// Exercise 3: 
a = intiger
b = intiger
c = intiger
d = intiger
e = intiger
f = average of a+b+c+d+e
g = minumum of a,b,c,d,e
h = maximum of a,b,c,d,e
i = sum

if a,b,c,d, or e ar not an intiger display "Please enter a valid number"

else
	a + b + c + d + e = i
	i / 5 = f
	display "f is the average"
then 
	if a > b,c,d,e then a = h else
	if b > a,c,d,e then b = h else
	if c > a,b,d,e then c = h else
	if d > a,b,c,e then d = h else
	if e > a,b,c,d then e = h
then display "h is the maximum"
then
	if a < b,c,d,e then a = g else
	if b < a,c,d,e then b = g else
	if c < a,b,d,e then c = g else
	if d < a,b,c,e then d = g else
	if e < a,b,c,d then e = g
then display "g is the minumum"
a) Read in 5 separate numbers.   
b) Calculate the average of the five numbers.
c) Find the smallest (minimum) and largest (maximum) of the five entered numbers.
d) Write out the results found from steps b and c with a message describing what they are

(Your pseudocode here)

-- END --
